"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["7297"],{4536(e,t,a){a.r(t),a.d(t,{metadata:()=>n,default:()=>p,frontMatter:()=>r,contentTitle:()=>l,toc:()=>d,assets:()=>c});var n=JSON.parse('{"id":"m13-cicd-release/quiz","title":"Quiz: CI/CD and Release","description":"<Quiz questions={[","source":"@site/docs/m13-cicd-release/quiz.mdx","sourceDirName":"m13-cicd-release","slug":"/m13-cicd-release/quiz","permalink":"/306-agentic-devops-claude/docs/m13-cicd-release/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: CI/CD and Release"},"sidebar":"courseSidebar","previous":{"title":"Lab: The Release Gate","permalink":"/306-agentic-devops-claude/docs/m13-cicd-release/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/306-agentic-devops-claude/docs/m13-cicd-release/deep-dive"}}'),o=a(4848),i=a(8453),s=a(7517);let r={sidebar_position:3,title:"Quiz: CI/CD and Release"},l="CI/CD & Release Engineering \u2014 Quiz",c={},d=[];function h(e){let t={h1:"h1",header:"header",...(0,i.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"cicd--release-engineering--quiz",children:"CI/CD & Release Engineering \u2014 Quiz"})}),"\n",(0,o.jsx)(s.A,{questions:[{prompt:"Your release pipeline runs an agent to analyze each candidate. The runner is ephemeral, holds shared registry and cloud credentials in environment variables, and executes dependencies you did not all write. What authority model should the agent's identity have here?",options:[{text:"Least privilege, read-only by default: it reads and renders, with no write path",correct:!0,explanation:"The CI runner is a rental car \u2014 ephemeral, holding keys that are not really yours, running code you did not all write. A compromised step inherits every permission the job holds, so the job should hold as little as possible. The agent lives in the analysis identity; publishing images and applying to prod are separate, separately-credentialed steps."},{text:"Full deploy permissions, but the prompt instructs it to deploy only after approval",correct:!1,explanation:"A prompt instruction is intent, not enforcement. In an untrusted runner, a compromised step that holds deploy permissions uses them regardless of what the prompt said. The write path must be structurally absent from the agent's identity, not merely discouraged."},{text:"A long-lived cloud admin key stored as a CI secret for whatever a run needs",correct:!1,explanation:"A stored long-lived key is the copied house key that opens the door forever \u2014 if it leaks from the shared runner it works until someone rotates it. Modern pipelines use OIDC for short-lived scoped credentials, and an analysis agent should not hold an admin key at all."},{text:"The same permissions as the deploy step, since both are one pipeline",correct:!1,explanation:"Collapsing the analysis and deploy identities into one destroys the least-privilege boundary. Same-pipeline is not a reason to share an identity; different jobs get different scopes, and the analysis step gets no write path."}]},{prompt:"You invoke the agent with claude -p --output-format json. The returned envelope has is_error set to true. Your gate reads .verdict from a schema-constrained result. What is the correct exit handling?",options:[{text:"Fail closed: treat the release as block, since no trustworthy verdict was produced",correct:!0,explanation:"The discipline is fail-closed. is_error true, a timeout from the wall-clock bound, or a result that does not validate against --json-schema all mean the same thing: the stage could not produce a trustworthy verdict, so it must block. An analysis you could not trust is not a green light."},{text:"Approve the release, because is_error only ever reports transient issues that do not block",correct:!1,explanation:"Approving on error is failing OPEN \u2014 the most dangerous default in a release gate. is_error true means the run did not complete trustworthily; you have no verdict, so proceeding ships an unreviewed change."},{text:"Retry the call with no time limit until is_error finally comes back false",correct:!1,explanation:"Unbounded retries defeat the wall-clock bound that makes the stage a pipeline stage. A persistent error must resolve to a fail-closed block, not an infinite loop. Bounded retries are fine, but the terminal state on repeated failure is block."},{text:"Parse the free-text plain text in the result and decide from that whether to proceed",correct:!1,explanation:"This throws away the reason for --output-format json and --json-schema. The pipeline gates on the structured verdict, not on plain text it re-interprets \u2014 and when is_error is true there is no valid verdict to read at all."}]},{prompt:"A release gate returns 0 for approve and non-zero for everything else. A reviewer argues that a missing candidate file and a blocked candidate are 'both just failures'. What does collapsing them cost?",options:[{text:"Nobody can tell a bad candidate from a broken gate, so the wrong person is sent to investigate",correct:!0,explanation:"Three outcomes exist because they have different owners. Exit 2 (block) sends a developer to their values file; exit 1 (cannot evaluate) sends somebody to the gate itself. Collapsed into one code, every failure looks like the candidate's fault, and a gate that has quietly stopped examining anything looks exactly like a strict one."},{text:"Nothing, provided the job fails in both cases \u2014 the merge is blocked either way, which is the outcome that matters",correct:!1,explanation:"The merge being blocked is necessary and not sufficient. The expensive part is diagnosis: a team that cannot distinguish 'your release is bad' from 'the gate is broken' debugs the wrong artifact, and the usual resolution is to weaken the gate."},{text:"Exit codes above 1 are reserved by POSIX, so using 2 for block is non-portable and will misbehave in CI",correct:!1,explanation:"Exit statuses 1-125 are freely available to a script. Using 2 for block is a convention this repository shares with its PreToolUse hook so every gate speaks one language."},{text:"CI systems retry on exit 1 but not on exit 2, so the distinction only matters for retry behaviour",correct:!1,explanation:"No CI system infers retry policy from an arbitrary exit code. The distinction is about who reads the failure and what they do next."}]},{prompt:"Northstar enforces separation of duties so the agent that proposes a change is not the agent that approves it. Which mechanisms make this enforcement STRUCTURAL rather than a matter of instruction? (Select all that apply.)",multiSelect:!0,options:[{text:"The change-reviewer role has no Edit or Write tool in its bundle at all",correct:!0,explanation:"The reviewer's independence is enforced by the absence of the capability: it is read-only by construction, so it physically cannot modify the candidate no matter what it decides. A reviewer that could edit the candidate would be a second author."},{text:"The platform-engineer proposer can Edit desired state and propose on a branch, but its definition forbids applying to the cluster",correct:!0,explanation:"The proposer carries a write tool (so it CAN author changes) but has no cluster-write path \u2014 it edits Git and stops at gitops-propose.sh. Proposing and deploying are held by different identities, which is the separation."},{text:"A single agent is told in its prompt to propose a change and then review its own work objectively",correct:!1,explanation:"This is intent, not enforcement \u2014 the exact anti-pattern the two-signature rule exists to prevent. One actor wearing two hats is not a review; asking one identity to be its own check is not separation of duties."},{text:"A human approval gate sits after the independent review for anything touching production",correct:!0,explanation:"The human gate is a distinct authority in the chain, recorded in the change contract, that no agent can satisfy on its own \u2014 a separate role that must sign before a production change moves."}]},{prompt:"During an incident, an operator points the agent at a 2024 health-verification runbook (owned by Platform, noting an endpoint 'added in v0.8.0') and asks it to fix the readiness probe. The agent proposes pointing the probe at /api/v1/status, citing the runbook. The app's source serves /healthz, /readyz, /api/v1/info \u2014 no /api/v1/status. What is the correct discipline?",options:[{text:"Reject the doc-anchored repair \u2014 the app source has no such route, so point the probe at /healthz and file the stale runbook as its own bug",correct:!0,explanation:"The runbook is a claim about the system; the code and pipeline ARE the system. The stale doc is more dangerous than wrong because it looks correct until checked. The repair renders and cites a source but points the probe at a 404. Executable evidence outranks documentation."},{text:"Trust the runbook \u2014 it is dated, owned by Platform, and cites when the endpoint was added",correct:!1,explanation:"The runbook's credibility is what makes the trap dangerous, not what makes it safe. Dated, owned, and provenance-noted describes a stale blueprint just as well as a current one. The source shows no /api/v1/status route, so the endpoint is fictional now."},{text:"Deploy the /api/v1/status change to production to test whether the endpoint exists",correct:!1,explanation:"This tests an unverified guess against live customers and violates the whole release chain. You can disprove the guess for free by reading the source or rendering the chart. Never validate a plausible-but-unchecked claim in production."},{text:"Split the difference: probe /api/v1/status with /health as a fallback path",correct:!1,explanation:"Splitting between two paths the app does not serve produces two 404s, not a fix. A readiness probe checks one path, and neither is a real route. The evidence points to exactly one correct path, /healthz."}]},{prompt:"After the reconciler deploys a canary, the agent reads: error_rate 0.037, p95_latency_ms 640, ready_replicas 1 of 2 desired. SLOs are error_rate <= 0.01 and p95 <= 300 ms. What verdict, and what does the agent do next?",options:[{text:"Verdict rollback with reasons \u2014 but it recommends, it does not roll back itself",correct:!0,explanation:"Any single breached signal is enough; here all three fail (error ~4x SLO, p95 >2x, a replica never Ready). The canary is the food taster showing trouble. The agent READS signals and RECOMMENDS; promoting or rolling back is a gated human-or-reconciler action."},{text:"Verdict promote \u2014 the service is up and responding, so 3.7% errors is fine for a canary",correct:!1,explanation:"A 3.7% error rate is nearly 4x the 1% SLO, p95 is over double budget, and half the replicas are not Ready \u2014 the signature of a bad release caught cheaply on a slice. Promoting pushes those failures to all traffic. The verdict is rollback."},{text:"Run the rollback itself immediately to restore service as fast as possible",correct:!1,explanation:"The agent produces the verdict; it does not execute the rollback. Promote and rollback are gated actions taken by a human or the reconciler \u2014 the same separation that keeps the proposer from deploying."},{text:"Ignore ready_replicas and decide only on the served error rate and latency",correct:!1,explanation:"Ready replicas is a first-class canary signal, not noise. One of two replicas never becoming Ready is not healthy even if served requests look fine \u2014 it is at half capacity. A signal you cannot confirm as healthy counts against promotion."}]},{prompt:"A pipeline step needs a cloud credential to push an image. Why does the module prefer OIDC federation over storing a long-lived cloud key as a CI secret?",options:[{text:"OIDC mints a short-lived credential scoped to this repo, branch, and job, expiring in minutes",correct:!0,explanation:"A stored long-lived key is a copied house key that opens the door forever. OIDC is a hotel keycard: the cloud vouches for this specific job and issues a credential bound to the exact repo, branch, and job, expiring at checkout. A leak from the runner opens nothing next week."},{text:"OIDC credentials are encrypted at rest in the CI secret store, unlike plaintext keys",correct:!1,explanation:"The security property is not encryption at rest \u2014 a stored key can be encrypted too. It is that the OIDC credential is short-lived and narrowly scoped, so even if stolen mid-run it is already expiring and useful only for one job."},{text:"OIDC lets you skip credentials entirely, since the runner needs no cloud identity",correct:!1,explanation:"OIDC does not remove the credential \u2014 the job still authenticates to the cloud. It replaces a long-lived stored key with a short-lived minted one. The identity still exists; it is just scoped and ephemeral."},{text:"OIDC keys never need rotation because they are permanent and centrally managed",correct:!1,explanation:"This is backwards \u2014 the point of OIDC is that credentials are ephemeral, not permanent. There is nothing long-lived to rotate because each credential expires in minutes on its own."}]},{prompt:"Why does the module run the agent headless (claude -p) with --output-format json rather than as an interactive session, for a pipeline stage?",options:[{text:"A stage takes one input, emits one structured result, and exits with a status code the next stage reads",correct:!0,explanation:"Interactive Claude is a colleague you chat with; headless Claude fills out a standardized form and drops it in the tray. The pipeline wants a filled form it can parse with jq and act on mechanically \u2014 a verdict, blockers, evidence \u2014 plus an exit code to branch on, not a conversation."},{text:"Headless mode is the only mode that can read files, which the analysis needs",correct:!1,explanation:"Both modes can read files. The difference is turn-taking and output structure: headless takes one prompt and emits one result non-interactively, which is what a stage needs \u2014 not a difference in file access."},{text:"Interactive sessions cost more per token, so headless is chosen to reduce the bill",correct:!1,explanation:"The reason is machine-consumability, not cost. A stage must parse a structured result and exit with a status code; a conversation gives neither. Token cost is not the driver here."},{text:"Headless mode disables the model's reasoning, making verdicts faster to produce",correct:!1,explanation:"Headless does not disable reasoning \u2014 the agent still analyzes fully. -p just means non-interactive: send the prompt, print the response, exit. The value is a parseable result and an exit code, not reduced reasoning."}]},{prompt:"The module runs Claude three ways on the same pull request: a shell step, a headless stage, and the GitHub Action. Which of these is true of the workflow that uses `anthropics/claude-code-action@v1` here? (Select all that apply.)",multiSelect:!0,options:[{text:"It carries continue-on-error and an explicit exit 0, so it can never fail or pass the merge",correct:!0,explanation:"The authority to block a merge lives in the deterministic workflow alone. The explaining job always succeeds, whatever the verdict \u2014 it runs after a decision, about a decision."},{text:"Its allow-list grants no Edit or Write, so it can read the repository but cannot change the candidate, the gate or the tests",correct:!0,explanation:'`--allowedTools "Read,Grep,Glob,Bash(gh pr comment:*)"` is the structural boundary. The turn cap is a budget; the allow-list is what stops an unattended agent editing the thing it is judging.'},{text:"It only runs when the deterministic gate has already returned a block",correct:!0,explanation:"The `if: steps.gate.outputs.rc == '2'` guard means no verdict, no model. That keeps cost proportional to problems rather than to pull requests."},{text:"It re-runs the analysers itself so it can form an independent second opinion on the release",correct:!1,explanation:"It reruns the same read-only gate command only to read the exit code and decide whether there is a block worth explaining. A second opinion that could disagree would mean two definitions of what blocks a release \u2014 which is the drift this module is built to avoid."}]},{prompt:"The pipeline wraps the agent call with the shell's timeout N and, when driving Claude via the Agent SDK, sets maxTurns. Why bound the stage two ways?",options:[{text:"A stage cannot run forever, so a wall-clock bound and a turn bound both cap the run and let it fail closed",correct:!0,explanation:"timeout is the CI-native wall-clock bound \u2014 no verdict in N seconds and the stage fails closed. maxTurns bounds the agent's reasoning loop when you drive it programmatically. Both exist so the stage terminates and, on hitting either limit, resolves to block rather than hanging."},{text:"The two bounds do the same thing, kept for redundancy in case one is misconfigured",correct:!1,explanation:"They bound different things: timeout caps wall-clock seconds, maxTurns caps the number of agent turns. A run can be slow in one turn or fast across many; each limit catches a different runaway. They are complementary, not redundant."},{text:"timeout controls token cost and maxTurns controls latency, so together they cap the bill",correct:!1,explanation:"Neither is primarily a cost control. Both exist so the stage terminates and fails closed if the analysis does not finish \u2014 a wall-clock cap and a turn cap. Cost is a side effect, not the design goal."},{text:"maxTurns is required for JSON output, and timeout is required for --bare mode",correct:!1,explanation:"Neither claim holds \u2014 JSON output and --bare work without these flags. maxTurns and timeout are independent bounds on run length, not prerequisites for output format or context mode."}]},{prompt:"In the canary stage the agent scrapes signals but the promote/rollback action is taken by a human or the reconciler. Some signals are missing (the scrape returned no p95). What should the fail-closed verdict be?",options:[{text:"Do not promote \u2014 an incomplete canary is not one that passed",correct:!0,explanation:"The verdict is fail-closed the same way the analysis gate is: incomplete or missing signals mean 'do not promote,' because a canary you could not fully verify is not a canary that passed. Absence of confirmation is not confirmation of health."},{text:"Promote \u2014 a missing signal means nothing breached it, so treat it as within budget",correct:!1,explanation:"Missing is not the same as within-budget. Treating an unmeasured signal as passing is failing open \u2014 you are promoting on evidence you do not have. Fail-closed means an unconfirmable signal counts against promotion."},{text:"Promote provisionally and let the reconciler re-check the missing signal after full rollout",correct:!1,explanation:"This ships the release to everyone before verifying it, defeating the point of a canary. The canary must fail cheaply and early on a slice; provisional promotion moves the risk to all traffic."},{text:"Return no verdict and leave the pipeline stage in a pending state indefinitely",correct:!1,explanation:"A stage that never resolves is a hung pipeline, not a safe one. The discipline is to resolve missing signals to a concrete fail-closed verdict \u2014 do not promote \u2014 not to leave the gate open forever."}]},{prompt:"You define a --json-schema release-verdict object with verdict, blockers, and evidence fields. The agent returns valid JSON, but it omits the blockers field the schema marks required. How should the gate treat this run?",options:[{text:"Fail closed \u2014 an off-contract result is untrustworthy, so treat it as block",correct:!0,explanation:"A malformed or off-contract answer fails validation instead of silently misleading the gate. The point of --json-schema is that the answer is not just JSON, it is YOUR JSON; a result missing a required field did not validate, and a verdict you could not validate blocks."},{text:"Accept it \u2014 the JSON parsed cleanly, so the verdict field alone is enough to gate on",correct:!1,explanation:"Parsing cleanly is not the same as validating against the schema. The schema exists precisely so an off-contract answer is rejected; accepting a result missing a required field reintroduces the ambiguity the schema removes."},{text:"Re-prompt the agent inline and merge whatever blockers the second call returns",correct:!1,explanation:"Silently patching an off-contract result papers over an untrustworthy run. The disciplined response is fail-closed: a result that did not validate is a block, not something to quietly repair mid-gate."},{text:"Infer the missing blockers from the evidence field so the schema check passes",correct:!1,explanation:"Inferring a required field defeats schema validation \u2014 you are manufacturing contract compliance the agent did not produce. A run that did not return a valid verdict blocks; you do not synthesize the missing part."}]}]})]})}function p(e={}){let{wrapper:t}={...(0,i.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(h,{...e})}):h(e)}},7517(e,t,a){a.d(t,{A:()=>s});var n=a(4848),o=a(6540);function i({q:e,state:t,onToggle:a}){return(0,n.jsxs)("div",{className:"question_QpiT",children:[(0,n.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,n.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,n.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,o)=>{let i=t.selected.has(o),s="option_qGol";return t.checked?e.correct?s+=" correct_LYA5":i&&(s+=" incorrect_kRFc"):i&&(s+=" chosen_H3WG"),(0,n.jsxs)("li",{children:[(0,n.jsxs)("button",{type:"button",className:s,disabled:t.checked,onClick:()=>a(o),children:[(0,n.jsx)("span",{className:"marker_vN_w",children:i?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(i||e.correct)&&e.explanation&&(0,n.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},o)})})]})}function s({questions:e}){let[t,a]=(0,o.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),l=t.filter((t,a)=>{var n,o;let i;return n=e[a],o=t.selected,i=n.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),n.multiSelect?o.size===i.length&&[...o].every(e=>n.options[e].correct):1===o.size&&i.includes([...o][0])}).length;return(0,n.jsxs)("div",{className:"quiz_I014",children:[e.map((o,s)=>(0,n.jsx)(i,{q:o,state:t[s],onToggle:t=>a(a=>a.map((a,n)=>{if(n!==s)return a;let o=new Set(a.selected);return e[s].multiSelect?o.has(t)?o.delete(t):o.add(t):(o.clear(),o.add(t)),{...a,selected:o}}))},s)),(0,n.jsxs)("div",{className:"actions_XUwt",children:[(0,n.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,n.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,n.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>s,x:()=>r});var n=a(6540);let o={},i=n.createContext(o);function s(e){let t=n.useContext(i);return n.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:s(e.components),n.createElement(i.Provider,{value:t},e.children)}}}]);