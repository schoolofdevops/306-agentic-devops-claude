"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["3534"],{9977(e,t,a){a.r(t),a.d(t,{metadata:()=>n,default:()=>p,frontMatter:()=>s,contentTitle:()=>c,toc:()=>h,assets:()=>l});var n=JSON.parse('{"id":"m12-containers-k8s-gitops/quiz","title":"Quiz: Kubernetes and GitOps","description":"<Quiz questions={[","source":"@site/docs/m12-containers-k8s-gitops/quiz.mdx","sourceDirName":"m12-containers-k8s-gitops","slug":"/m12-containers-k8s-gitops/quiz","permalink":"/306-agentic-devops-claude/docs/m12-containers-k8s-gitops/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Kubernetes and GitOps"},"sidebar":"courseSidebar","previous":{"title":"Lab: Harden and Ship an Image","permalink":"/306-agentic-devops-claude/docs/m12-containers-k8s-gitops/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/306-agentic-devops-claude/docs/m12-containers-k8s-gitops/deep-dive"}}'),o=a(4848),i=a(8453),r=a(7517);let s={sidebar_position:3,title:"Quiz: Kubernetes and GitOps"},c="Containers, Kubernetes & GitOps \u2014 Quiz",l={},h=[];function d(e){let t={h1:"h1",header:"header",...(0,i.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"containers-kubernetes--gitops--quiz",children:"Containers, Kubernetes & GitOps \u2014 Quiz"})}),"\n",(0,o.jsx)(r.A,{questions:[{prompt:"An agent reviews a Helm chart and reports the templates and values.yaml look correct. Why does the module insist the real review runs `helm template` and reads the RENDERED manifest instead?",options:[{text:"Defects like a wrong probe path or a 100% maxUnavailable only appear once template and values are merged",correct:!0,explanation:"The rendered manifest is template plus values combined. A probe pointing at the wrong path or a self-inflicting rollout number is invisible in either half alone \u2014 it exists only in the merged output, which is what actually gets applied."},{text:"Reading templates is slower for an agent than running a single command, so rendering is chosen purely for speed and convenience",correct:!1,explanation:"Speed is not the reason. Rendering exposes a class of defects that reading the template can never reveal, regardless of how fast you read."},{text:"The values.yaml file is encrypted at rest, so the only way to see its real contents is through the templating engine that decrypts it",correct:!1,explanation:"values.yaml is plain YAML, not encrypted. The reason to render is that defects live in the combination, not because values are hidden."},{text:"helm template validates the chart against the live cluster and refuses to emit YAML if the current running state would reject it on apply",correct:!1,explanation:"helm template renders locally with no cluster contact. It does not check against running state \u2014 that is a separate diff step you run afterward."}]},{prompt:"The orders-api chart renders a pod with no `resources` block \u2014 no requests, no limits. Read as effect rather than intent, what is the real problem?",options:[{text:"The pod can schedule onto a node with no room and grow until it OOM-kills its neighbors",correct:!0,explanation:"No requests means the scheduler has no signal about how much room the pod needs; no limits means it can consume memory until it starves neighbors and gets OOM-killed under load. Absence is a scheduling and stability decision made by omission."},{text:"The chart will fail to render because Kubernetes requires every container to declare at least one CPU and memory limit before templating",correct:!1,explanation:"Kubernetes does not require a resources block; the pod renders and runs fine at low load. The danger is what happens under pressure, not at render time."},{text:"Argo CD refuses to sync any manifest missing a resources block, so the deployment is permanently stuck out of sync with Git",correct:!1,explanation:"Argo CD syncs the manifest as written; a missing resources block is valid YAML. Nothing blocks the sync \u2014 the problem surfaces only under real load."},{text:"The absence is harmless because Kubernetes applies sensible default requests and limits to any container that omits them",correct:!1,explanation:"There are no automatic defaults unless a LimitRange is configured in the namespace. Without one, an omitted block means genuinely no requests and no limits."}]},{prompt:"A Dockerfile builds from `python:latest`, copies the whole repo with `COPY . .`, and never sets a USER. Which packing-list problems does this introduce? (Select all that apply.)",multiSelect:!0,options:[{text:"The floating base tag means the tested image and the shipped image can be silently different builds",correct:!0,explanation:"latest is a moving target. The image you tested and the one that ships later can be different builds \u2014 a change you did not make, applied at a time you did not choose."},{text:"The `COPY . .` can sweep a .env or credential file into a permanent, inspectable image layer",correct:!0,explanation:"Image layers are not scratch space. A blanket copy can pull a secret into a layer, and that credential ships sealed inside the image to every environment."},{text:"With no USER directive the process runs as root, which becomes root on the node if it escapes its namespace",correct:!0,explanation:"A container running as root runs as root on the node if it breaks out of its namespace. A USER directive dropping to an unprivileged account is the packing-list fix."},{text:"The build fails immediately because Docker rejects any Dockerfile that both uses latest and omits a USER instruction",correct:!1,explanation:"Docker builds this file without complaint. That is exactly the danger \u2014 every flaw seals in cleanly and ships unopened, because nothing at build time forces you to catch it."}]},{prompt:"The lesson calls kind a flight simulator rather than a toy. What is the precise sense in which that analogy holds for agentic work?",options:[{text:"It runs the same API server and kubelet as production, so a proposed change can prove itself safely before touching anything real",correct:!0,explanation:"kind is a real cluster \u2014 same control plane, same kubelet \u2014 where an agent change can crash safely and be reset in seconds. The point is a place to prove a change before it touches production."},{text:"It is a mock cluster that returns canned API responses, so it costs nothing and never actually schedules any of the pods you deploy",correct:!1,explanation:"kind is not a mock; it really schedules pods on real nodes. That is why it can surface genuine bugs a fake API server would miss."},{text:"It automatically mirrors production state into the laptop, so whatever you validate on kind is guaranteed to behave identically in the real cluster",correct:!1,explanation:"kind mirrors nothing automatically, and validation on one profile does not guarantee the other. The value is a safe place to break things, not a guarantee of identical behavior."},{text:"It is the only environment where an agent is allowed a cluster-write path, so all agent applies are permitted directly against it",correct:!1,explanation:"The golden path keeps the agent off the direct write path everywhere. kind is where changes prove themselves, not where the agent gets a write key."}]},{prompt:"A change validated on the lean single-node kind profile is promoted, but the standard two-node profile has a separate control-plane and worker. Why can't you assume the change is valid on both?",options:[{text:"The profile changes the topology, so scheduling behavior differs between one shared node and a dedicated worker",correct:!0,explanation:"Lean packs control plane and workload on one node; standard separates them. A node selector or a resource request that fits one topology may be unschedulable on the other \u2014 the profile is a real difference, not a cosmetic one."},{text:"The lean profile runs a completely different Kubernetes distribution, so its API objects are incompatible with the standard cluster's control plane",correct:!1,explanation:"Both profiles run the same Kubernetes; only the node topology differs. The risk is scheduling behavior across nodes, not API incompatibility."},{text:"The lean profile disables RBAC entirely, so any manifest that relies on service-account permissions silently breaks on the standard profile",correct:!1,explanation:"RBAC is present on both; the lean profile does not disable it. The topology, not the auth model, is what changes between profiles."},{text:"Manifests are stored per-profile, so a change made on lean is physically absent from the standard profile until manually copied across",correct:!1,explanation:"The manifests are the same source; profiles differ in cluster shape, not in where desired state lives. Topology-driven scheduling is the real concern."}]},{prompt:"Northstar's read-only kubeconfig binds a ClusterRole whose verbs are exactly get, list, and watch. Why does the lesson call this enforcement rather than a promise?",options:[{text:"The API server rejects any write from that identity on every call, so it cannot write regardless of intent",correct:!0,explanation:"The verbs define what the identity can do, checked by the API server on every request. A write is refused at the target \u2014 that is enforcement, not a label the agent is trusted to honor."},{text:"The kubeconfig file is marked read-only on disk, so the agent process is unable to open it for writing and therefore cannot issue any commands",correct:!1,explanation:"File permissions on the kubeconfig are unrelated. Enforcement comes from the API server checking the bound verbs on each call, not from the file mode."},{text:"A comment at the top of the kubeconfig instructs the agent to only read, and the agent's system prompt reliably follows written instructions like that",correct:!1,explanation:"An instruction is intent the model interprets, the weakest control. The verbs are enforced by the API server, which is why a write is physically refused."},{text:"The read-only role is applied only in kind, so in production the same identity silently gains full create and delete permissions on the cluster",correct:!1,explanation:"The read-only binding is the point precisely because it holds where it is applied. It does not silently grant writes elsewhere \u2014 the verbs are the boundary."}]},{prompt:"The northstar namespace carries Pod Security labels `enforce: baseline`, `warn: restricted`, `audit: restricted`. What does `enforce: baseline` actually do to a pod that violates the baseline standard?",options:[{text:"The API server rejects the pod at creation, before it ever schedules",correct:!0,explanation:"enforce is an admission-time gate: a pod violating the baseline standard is refused at creation by the API server, no matter who submitted it. That is enforcement in its purest form."},{text:"The pod is created and scheduled normally but a warning event is attached to it for an operator to review and remediate later on",correct:!1,explanation:"That describes warn, not enforce. enforce rejects the object outright at admission; it does not let a violating pod exist with a warning."},{text:"The pod runs to completion but is logged to an audit trail so violations can be counted and reported without ever blocking a workload",correct:!1,explanation:"That is the audit level. enforce is different: it blocks the pod at creation rather than recording a violation after the fact."},{text:"The scheduler quietly rewrites the pod spec to comply with baseline, so the workload runs in a modified form the author never wrote",correct:!1,explanation:"Pod Security admission does not rewrite specs; it accepts or rejects them. A baseline violation under enforce is rejected, not silently patched."}]},{prompt:"The `gitops-propose.sh` script stages a desired-state change, prints the diff, and then stops without touching any cluster. Why is that stopping point built into the script itself?",options:[{text:"It makes the agent's authority end structurally at the proposal, with no cluster-write path to fat-finger",correct:!0,explanation:"The script refuses to touch a cluster by design, so the agent's most dangerous action is proposing a bad change in Git \u2014 which a human reads before it syncs. The stop is a structural boundary, not a courtesy."},{text:"The script pauses only to let the agent double-check its own diff, after which it is expected to run the apply itself once satisfied",correct:!1,explanation:"The agent never applies. The stop is permanent for the agent \u2014 the reconciler is the only writer, and a human approves before sync."},{text:"Argo CD requires a manual confirmation prompt between staging and sync, and the script is simply forwarding that prompt to the agent",correct:!1,explanation:"The stop is in the propose script, not an Argo CD prompt relayed to the agent. Argo CD syncs from merged Git state, not from an agent confirmation."},{text:"Stopping saves API-server round trips, which keeps the agent under its per-turn token budget during large reviews",correct:!1,explanation:"The reason is safety, not tokens. The script stops because the agent has no direct cluster-write path, not to economize on requests."}]},{prompt:"Someone hand-edits a live Deployment directly with kubectl, drifting it away from what Git declares. On the golden path with Argo CD watching, what happens next?",options:[{text:"The reconciler pulls the cluster back to match Git, treating Git as the source of truth",correct:!0,explanation:"Argo CD continuously reconciles the cluster to the merged Git state. A hand edit is drift, and the reconciler pulls it back \u2014 the cluster is a projection of Git, not the other way around."},{text:"The hand edit becomes the new source of truth, and Argo CD updates the Git repository to record whatever the operator changed on the live object",correct:!1,explanation:"Argo CD never writes the cluster state back into Git. Git is the source of truth; a live edit is drift to be corrected, not a new baseline."},{text:"Argo CD halts all reconciliation until an operator manually resolves the conflict between the edited object and the Git declaration by hand",correct:!1,explanation:"Reconciliation does not halt on drift; that is the case Argo CD is built to handle. It converges the cluster back to Git automatically."},{text:"The edit persists because kubectl writes bypass the reconciler, which only ever acts on objects it originally created from scratch",correct:!1,explanation:"The reconciler manages the tracked objects and corrects drift on them regardless of who wrote the change. A direct kubectl edit does not escape reconciliation."}]},{prompt:"A teammate says the golden path is heavy ceremony: candidate, approval, merge, sync, verify. What does that ceremony actually buy compared to giving the agent kubectl?",options:[{text:"It makes an accidental production change impossible \u2014 the worst the agent can do is a pull request someone rejects",correct:!0,explanation:"With no direct write path there is no apply to fat-finger and no upgrade for a runaway loop. The blast radius of an agent mistake is bounded to a rejected pull request, which is the whole payoff."},{text:"It guarantees the agent's proposed changes are always correct, because the rendering and merge steps automatically fix any mistakes the agent made",correct:!1,explanation:"Nothing makes the agent correct; a human still reviews. The ceremony bounds the damage of a wrong proposal, it does not prevent wrong proposals."},{text:"It lets the agent apply directly as long as a human has approved the branch, which removes the reconciler from the write path for speed",correct:!1,explanation:"The reconciler is always the only writer; approval does not hand the agent a key. Removing the reconciler would defeat the entire model."},{text:"It eliminates the need to review the rendered diff, since merging to main is itself the approval that certifies the change is safe",correct:!1,explanation:"The rendered diff is exactly what the reviewer approves before merge. The diff review is the substance of the approval step, not something the ceremony removes."}]},{prompt:"The Deep Dive reintroduces the M1 readiness bug one layer up: the app serves `/healthz` but the rendered probe points at `/health`. Why is this failure especially expensive to diagnose?",options:[{text:"The app is healthy but the pods never go ready, so it looks like an outage with the wrong cause",correct:!0,explanation:"Separating application health from probe config from rollout state is the skill. A healthy app whose pods never pass readiness reads as an outage, and chasing the app instead of the probe turns a thirty-minute incident into a three-hour one."},{text:"The probe misconfiguration corrupts the container image itself, so the only fix is rebuilding and repushing the image through CI before any retry",correct:!1,explanation:"A probe path is manifest configuration, not part of the image. The image is fine; the fix is in the rendered manifest through Git, no rebuild needed."},{text:"Kubernetes silently routes traffic to the not-ready pods anyway, so users see intermittent errors that never correlate with the readiness state",correct:!1,explanation:"Not-ready pods are pulled from the Service endpoints, so they do not receive traffic. The expense is the misleading symptom, not phantom routing to unready pods."},{text:"The wrong probe path triggers an immediate cluster-wide rollback of every workload, so the blast radius extends far beyond the one service",correct:!1,explanation:"A bad probe stalls the one rollout; it does not roll back the whole cluster. The difficulty is telling probe failure apart from real application failure."}]},{prompt:"An agent is genuinely good at the container packing-list review but the Change Gate still denies `docker push` from its identity. Why draw the line exactly there?",options:[{text:"Reviewing files is safe pattern-matching, but pushing an image is a write to a shared artifact that ships everywhere",correct:!0,explanation:"The agent holds every relevant file in context and never tires of the checklist \u2014 that is where its advantage is. Pushing seals and distributes the artifact to every environment, so that write goes through CI, denied from the agent identity by the Change Gate."},{text:"The agent lacks the network libraries required to reach a registry, so the Change Gate is a formality that documents a capability the agent never had",correct:!1,explanation:"The denial is a real enforcement decision, not a formality. The Change Gate blocks the push because it is a distributing write, regardless of whether the agent could technically reach a registry."},{text:"docker push is denied only in kind, and in production the agent is expected to push images directly once the review passes cleanly",correct:!1,explanation:"The agent does not push in any environment. Image distribution goes through CI everywhere; the review is the agent's job, the push is not."},{text:"Pushing is denied because it consumes more tokens than a review, and the Change Gate exists to keep agent operations within a cost budget",correct:!1,explanation:"The line is drawn on safety, not cost. A push seals a flaw into an artifact that ships unopened everywhere, which is why it is kept off the agent identity."}]}]})]})}function p(e={}){let{wrapper:t}={...(0,i.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(d,{...e})}):d(e)}},7517(e,t,a){a.d(t,{A:()=>r});var n=a(4848),o=a(6540);function i({q:e,state:t,onToggle:a}){return(0,n.jsxs)("div",{className:"question_QpiT",children:[(0,n.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,n.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,n.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,o)=>{let i=t.selected.has(o),r="option_qGol";return t.checked?e.correct?r+=" correct_LYA5":i&&(r+=" incorrect_kRFc"):i&&(r+=" chosen_H3WG"),(0,n.jsxs)("li",{children:[(0,n.jsxs)("button",{type:"button",className:r,disabled:t.checked,onClick:()=>a(o),children:[(0,n.jsx)("span",{className:"marker_vN_w",children:i?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(i||e.correct)&&e.explanation&&(0,n.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},o)})})]})}function r({questions:e}){let[t,a]=(0,o.useState)(e.map(()=>({selected:new Set,checked:!1}))),s=t.every(e=>e.checked),c=t.filter((t,a)=>{var n,o;let i;return n=e[a],o=t.selected,i=n.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),n.multiSelect?o.size===i.length&&[...o].every(e=>n.options[e].correct):1===o.size&&i.includes([...o][0])}).length;return(0,n.jsxs)("div",{className:"quiz_I014",children:[e.map((o,r)=>(0,n.jsx)(i,{q:o,state:t[r],onToggle:t=>a(a=>a.map((a,n)=>{if(n!==r)return a;let o=new Set(a.selected);return e[r].multiSelect?o.has(t)?o.delete(t):o.add(t):(o.clear(),o.add(t)),{...a,selected:o}}))},r)),(0,n.jsxs)("div",{className:"actions_XUwt",children:[(0,n.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,n.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),s&&(0,n.jsxs)("span",{className:"score_AN7p",children:["Score: ",c," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>r,x:()=>s});var n=a(6540);let o={},i=n.createContext(o);function r(e){let t=n.useContext(i);return n.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function s(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:r(e.components),n.createElement(i.Provider,{value:t},e.children)}}}]);