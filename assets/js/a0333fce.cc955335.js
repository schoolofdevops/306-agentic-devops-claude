"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["7634"],{4828(e,t,n){n.r(t),n.d(t,{metadata:()=>o,default:()=>u,frontMatter:()=>a,contentTitle:()=>h,toc:()=>d,assets:()=>l});var o=JSON.parse('{"id":"m8-hooks-policy-gitops/lesson","title":"Hooks and Guardrails","description":"Every module so far has made the agent better behaved. You wrote CLAUDE.md rules it reads, permission settings it respects, skills that steer how it works, and typed tools that narrow what it reaches for. All of those share one property a control that did not ask the agent to behave, but removed the capability entirely. This module is about that shift, made general. Because there is a category of action where \\"the agent chose correctly\\" is not good enough, where you need a control that holds even if the model misreads the ticket, ignores the rule, or is talked into the wrong thing. A terraform apply on production. A kubectl delete namespace. A command that prints your database password into the transcript. For those, hope is not a control. This module builds the layer that does not depend on the agent\'s judgment at all.","source":"@site/docs/m8-hooks-policy-gitops/lesson.md","sourceDirName":"m8-hooks-policy-gitops","slug":"/m8-hooks-policy-gitops/lesson","permalink":"/306-agentic-devops-claude/docs/m8-hooks-policy-gitops/lesson","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":1,"frontMatter":{"sidebar_position":1,"title":"Hooks and Guardrails"},"sidebar":"courseSidebar","previous":{"title":"Deep Dive (Part 2)","permalink":"/306-agentic-devops-claude/docs/m7-tool-interface-engineering/deep-dive"},"next":{"title":"Lab: The Change Gate","permalink":"/306-agentic-devops-claude/docs/m8-hooks-policy-gitops/lab"}}'),s=n(4848),r=n(8453),i=n(7684);let a={sidebar_position:1,title:"Hooks and Guardrails"},h="Hooks and Guardrails",l={},d=[{value:"Why this is a different kind of control",id:"why-this-is-a-different-kind-of-control",level:2},{value:"Model the threat surface first",id:"model-the-threat-surface-first",level:2},{value:"The command layer: deterministic hooks",id:"the-command-layer-deterministic-hooks",level:2},{value:"The target layer: least privilege at the identity",id:"the-target-layer-least-privilege-at-the-identity",level:2},{value:"The change path: GitOps and the reconciler-only writer",id:"the-change-path-gitops-and-the-reconciler-only-writer",level:2},{value:"The Environment Contract: making the target explicit",id:"the-environment-contract-making-the-target-explicit",level:2},{value:"How the layers compose",id:"how-the-layers-compose",level:2},{value:"What you&#39;ll do in the lab",id:"what-youll-do-in-the-lab",level:2}];function c(e){let t={admonition:"admonition",code:"code",em:"em",h1:"h1",h2:"h2",header:"header",li:"li",mermaid:"mermaid",ol:"ol",p:"p",pre:"pre",strong:"strong",table:"table",tbody:"tbody",td:"td",th:"th",thead:"thead",tr:"tr",ul:"ul",...(0,r.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(i.A,{src:"decks/m8-hooks-policy-gitops.html",title:"M8: Hooks and Guardrails"}),"\n",(0,s.jsx)(t.header,{children:(0,s.jsx)(t.h1,{id:"hooks-and-guardrails",children:"Hooks and Guardrails"})}),"\n",(0,s.jsxs)(t.p,{children:["Every module so far has made the agent ",(0,s.jsx)(t.em,{children:"better behaved"}),". You wrote CLAUDE.md rules it reads, permission settings it respects, skills that steer how it works, and typed tools that narrow what it reaches for. All of those share one property: they work by getting the model to ",(0,s.jsx)(t.em,{children:"choose"}),' the right action. Module 7\'s read-only observer was the first crack in that pattern: a control that did not ask the agent to behave, but removed the capability entirely. This module is about that shift, made general. Because there is a category of action where "the agent chose correctly" is not good enough, where you need a control that holds ',(0,s.jsx)(t.em,{children:"even if the model misreads the ticket, ignores the rule, or is talked into the wrong thing."})," A ",(0,s.jsx)(t.code,{children:"terraform apply"})," on production. A ",(0,s.jsx)(t.code,{children:"kubectl delete namespace"}),". A command that prints your database password into the transcript. For those, hope is not a control. This module builds the layer that does not depend on the agent's judgment at all."]}),"\n",(0,s.jsxs)(t.p,{children:["Here is the concrete situation it closes. A ticket lands during an incident: ",(0,s.jsx)(t.em,{children:'"clean up the old resources and fix production."'}),' Your agent has CLAUDE.md rules saying "never apply to production directly" and "route changes through GitOps." Those rules are real and the agent usually follows them. But ',(0,s.jsx)(t.em,{children:"usually"}),' is doing a lot of work in that sentence. Under time pressure, with an ambiguous instruction, the model might read "fix production" as "run the apply", and every intent control you have is a rule it is ',(0,s.jsx)(t.em,{children:"interpreting"}),", which means a rule it can misinterpret. What you want is a control that makes the destructive apply ",(0,s.jsx)(t.em,{children:"fail"}),", deterministically, regardless of what the model decided. That control is a hook, and it is the center of this module."]}),"\n",(0,s.jsx)(t.h2,{id:"why-this-is-a-different-kind-of-control",children:"Why this is a different kind of control"}),"\n",(0,s.jsxs)(t.p,{children:["The reason this matters at work is that the failure modes of intent controls and enforcement controls are completely different, and you need both because each covers the other's gap. An ",(0,s.jsx)(t.strong,{children:"intent control"})," (a CLAUDE.md rule, a skill instruction, a permission preference) steers what the agent ",(0,s.jsx)(t.em,{children:"tries"})," to do. It fails when the model misinterprets: the rule was there, the agent read it, and it still did the wrong thing because it understood the situation differently than you did. An ",(0,s.jsx)(t.strong,{children:"enforcement control"})," (a hook, an IAM policy, an RBAC role, an admission webhook) physically prevents the action regardless of intent. It fails only when it is ",(0,s.jsx)(t.em,{children:"misconfigured"}),": the rule was too narrow, the pattern did not match, you forgot a case."]}),"\n",(0,s.jsxs)(t.p,{children:["The distinction is not academic. Intent controls fail ",(0,s.jsx)(t.em,{children:"silently and plausibly"}),": the agent does something reasonable-looking that happens to be wrong, and you find out later. Enforcement controls fail ",(0,s.jsx)(t.em,{children:"loudly and structurally"}),': either the action was blocked or it was not, and you can test exactly which. You want the destructive operations behind enforcement, so that "did the safety hold?" is a question you can answer with a test instead of a hope.']}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.strong,{children:"Analogy:"})," think about how a building keeps people out of the server room. There is a ",(0,s.jsx)(t.strong,{children:"sign on the door"}),' that says "Authorized personnel only." That is an intent control: it works on people who read it and choose to comply, and it fails the moment someone decides the sign does not apply to them, or does not notice it, or is in a hurry. Then there is the ',(0,s.jsx)(t.strong,{children:"badge reader"})," that will not open the door without an authorized badge. That is an enforcement control: it does not care whether you read the sign or how urgent your reason is; the door does not open. You do not remove the sign, it sets expectations and handles the honest majority cheaply. But for the room that actually matters, you do not rely on the sign ",(0,s.jsx)(t.em,{children:"alone"}),". You put a badge reader on it, because the sign fails exactly when you most need it to hold: when someone is motivated to ignore it. Intent controls are the sign. Enforcement controls are the badge reader. This module is about putting badge readers on the doors that matter."]}),"\n",(0,s.jsx)(t.mermaid,{value:'flowchart TD\n    subgraph INTENT["Intent controls: the agent chooses to comply"]\n      I1["CLAUDE.md rules"]\n      I2["Skill instructions"]\n      I3["Permission preferences"]\n      I1 --\x3e IF["Fails when the model<br/>MISINTERPRETS<br/>(silent, plausible)"]\n      I2 --\x3e IF\n      I3 --\x3e IF\n    end\n    subgraph ENFORCE["Enforcement controls: the action is physically prevented"]\n      E1["Hooks (deterministic scripts)"]\n      E2["IAM / RBAC identities"]\n      E3["Admission controllers"]\n      E4["GitOps reconciler-only writes"]\n      E1 --\x3e EF["Fails only when<br/>MISCONFIGURED<br/>(loud, testable)"]\n      E2 --\x3e EF\n      E3 --\x3e EF\n      E4 --\x3e EF\n    end\n    style IF fill:#fff3e0,stroke:#f57c00\n    style EF fill:#e8f5e9,stroke:#388e3c'}),"\n",(0,s.jsxs)(t.p,{children:["Read the two columns as ",(0,s.jsx)(t.em,{children:"layers, not alternatives"}),". You keep every intent control you built, they are cheap, they handle the common case, and they make the agent pleasant to work with. Enforcement controls sit ",(0,s.jsx)(t.em,{children:"underneath"})," them as the floor: the thing that catches what intent missed. The rest of this module builds that floor at three depths: the command layer (hooks), the target layer (identities, RBAC, admission), and the change path (GitOps), and then ties them together with the Environment Contract."]}),"\n",(0,s.jsx)(t.h2,{id:"model-the-threat-surface-first",children:"Model the threat surface first"}),"\n",(0,s.jsxs)(t.p,{children:['Before building controls, name what you are defending against. The threat surface for an agent doing ops is not "the model is evil". It is far more mundane and far more likely: the model does something ',(0,s.jsx)(t.em,{children:"reasonable given how it read the situation"})," that turns out to be destructive. Four patterns cover most of it, and every control in this module maps to one."]}),"\n",(0,s.jsxs)(t.table,{children:[(0,s.jsx)(t.thead,{children:(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.th,{children:"Threat pattern"}),(0,s.jsx)(t.th,{children:"What it looks like"}),(0,s.jsx)(t.th,{children:"The reasonable-but-wrong reading"})]})}),(0,s.jsxs)(t.tbody,{children:[(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:(0,s.jsx)(t.strong,{children:"Wrong target"})}),(0,s.jsx)(t.td,{children:"A correct command against the wrong environment"}),(0,s.jsx)(t.td,{children:'"Fix the deployment", agent is pointed at prod, thought it was staging'})]}),(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:(0,s.jsx)(t.strong,{children:"Excess blast radius"})}),(0,s.jsx)(t.td,{children:"A command whose scope is wider than intended"}),(0,s.jsxs)(t.td,{children:['"Restart the pods" becomes ',(0,s.jsx)(t.code,{children:"--all-namespaces"})]})]}),(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:(0,s.jsx)(t.strong,{children:"Irreversible destruction"})}),(0,s.jsx)(t.td,{children:"A delete/apply with no undo"}),(0,s.jsx)(t.td,{children:'"Clean up old resources" deletes a live namespace'})]}),(0,s.jsxs)(t.tr,{children:[(0,s.jsx)(t.td,{children:(0,s.jsx)(t.strong,{children:"Data leakage"})}),(0,s.jsx)(t.td,{children:"Secrets pulled into the transcript"}),(0,s.jsx)(t.td,{children:'"Check the DB config" dumps a Secret\'s decoded value'})]})]})]}),"\n",(0,s.jsxs)(t.p,{children:['None of these require a malicious model. Each is a plausible misreading of an ambiguous instruction, which is exactly why an intent control ("don\'t do that") is a weak defense: the agent is not ',(0,s.jsx)(t.em,{children:"trying"})," to violate the rule, it genuinely believes it is doing the right thing. Enforcement is the answer precisely because it does not depend on the agent's belief being correct."]}),"\n",(0,s.jsx)(t.h2,{id:"the-command-layer-deterministic-hooks",children:"The command layer: deterministic hooks"}),"\n",(0,s.jsxs)(t.p,{children:["A ",(0,s.jsx)(t.strong,{children:"hook"})," in Claude Code is a shell command that runs ",(0,s.jsx)(t.em,{children:"around"})," a tool call: before it (",(0,s.jsx)(t.code,{children:"PreToolUse"}),"), after it (",(0,s.jsx)(t.code,{children:"PostToolUse"}),"), or at other lifecycle points. The defining property, and the reason this module exists: ",(0,s.jsx)(t.strong,{children:"a hook is a deterministic shell script, not an LLM."})," It does not reason, interpret, or get talked out of its decision. It runs the same code on the same input every time and returns the same verdict. When you gate the agent's ",(0,s.jsx)(t.code,{children:"Bash"})," calls through a hook, you have inserted a piece of ordinary, testable software between the model's intent and the command's execution."]}),"\n",(0,s.jsxs)(t.p,{children:["A ",(0,s.jsx)(t.code,{children:"PreToolUse"})," hook receives the proposed tool call as JSON on stdin, the tool name and its input, and gets to decide before anything runs. It blocks by exiting non-zero (and, in current Claude Code, by emitting a JSON decision of ",(0,s.jsx)(t.code,{children:'permissionDecision: "deny"'}),"). The critical design choice is ",(0,s.jsx)(t.strong,{children:"fail-closed"}),": if the hook script itself errors (jq missing, malformed input, an unhandled case) it must ",(0,s.jsx)(t.em,{children:"block"}),', not allow. A gate that cannot evaluate a command has no business letting it through. "I could not decide" and "deny" must be the same outcome, because the alternative is a safety control that opens the door whenever it is confused.']}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.strong,{children:"Analogy:"})," a hook is the airport security checkpoint, and fail-closed is its operating rule. The checkpoint runs the ",(0,s.jsx)(t.em,{children:"same"})," procedure on every passenger: it does not decide who looks trustworthy, it applies fixed rules to what the scanner sees. And when the scanner malfunctions or the operator cannot tell what an object is, the rule is ",(0,s.jsx)(t.em,{children:"not"}),' "wave them through to keep the line moving". It is "stop, do not pass, resolve it." The default on uncertainty is closed, not open. A checkpoint that let people through whenever its equipment glitched would not be a security control at all. Your hook is the same: deterministic procedure, and closed on doubt.']}),"\n",(0,s.jsx)(t.mermaid,{value:'sequenceDiagram\n    participant A as Agent\n    participant CC as Claude Code\n    participant H as change-gate.sh (hook)\n    participant B as Bash\n    A->>CC: proposes Bash("terraform apply ... production")\n    CC->>H: PreToolUse event (tool_name, command) on stdin\n    Note over H: deterministic rules,<br/>no model, no interpretation\n    alt command matches a deny rule OR hook errors\n        H--\x3e>CC: permissionDecision: deny (exit 2)\n        CC--\x3e>A: BLOCKED + reason\n        Note over B: command never runs\n    else command is safe\n        H--\x3e>CC: exit 0\n        CC->>B: run the command\n        B--\x3e>A: result\n    end'}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.strong,{children:"Change Gate"})," you build in the lab is one such hook, and it encodes the threat table directly. It denies, deterministically: a ",(0,s.jsx)(t.code,{children:"terraform apply"})," on production (irreversible + wrong-layer, route it through GitOps), a delete of a protected namespace (irreversible destruction), a mutating ",(0,s.jsx)(t.code,{children:"kubectl"})," with an all-namespaces target (excess blast radius), a command that would render a Secret's decoded value (data leakage), and a ",(0,s.jsx)(t.code,{children:"docker push"})," (images ship through CI, not an agent identity). Each denial returns a ",(0,s.jsx)(t.em,{children:"reason"})," the agent sees, so it can correct course rather than just hitting a wall. And crucially, the gate is ",(0,s.jsx)(t.em,{children:"narrow"}),": ",(0,s.jsx)(t.code,{children:"terraform plan"})," passes, a dev apply passes, a read-only ",(0,s.jsx)(t.code,{children:"kubectl get -A"})," passes, a Secret referenced ",(0,s.jsx)(t.em,{children:"by name"})," passes. A gate that blocks investigation along with destruction is a broken gate, the whole point is to stop the four dangerous command patterns while leaving the agent free to do its job."]}),"\n",(0,s.jsx)(t.admonition,{title:"A hook is code, so you test it like code",type:"note",children:(0,s.jsxs)(t.p,{children:["Because the gate is a deterministic script, its correctness is ",(0,s.jsx)(t.em,{children:"testable"}),' in a way an intent control never is. You cannot write a unit test for "the model will interpret this rule correctly." You absolutely can write one for "this exact command produces exit code 2." The lab has you drive the gate with real deny inputs and real allow inputs and assert the verdicts, the same positive/negative/boundary testing you applied to skills in Module 6, now applied to a safety control. An enforcement control you have tested is worth more than an intent control you are hoping about.']})}),"\n",(0,s.jsx)(t.h2,{id:"the-target-layer-least-privilege-at-the-identity",children:"The target layer: least privilege at the identity"}),"\n",(0,s.jsxs)(t.p,{children:["The hook is the command layer, it inspects what the agent is ",(0,s.jsx)(t.em,{children:"about to run"}),". Underneath it sits a second, stronger layer: what the agent's ",(0,s.jsx)(t.em,{children:"identity is even allowed to do"}),", enforced by the target system rather than by your script. This is the Module 7 observer idea generalized from tools to credentials. If the kubeconfig the agent holds is bound to a role that has only ",(0,s.jsx)(t.code,{children:"get"}),", ",(0,s.jsx)(t.code,{children:"list"}),", ",(0,s.jsx)(t.code,{children:"watch"})," verbs, then the agent ",(0,s.jsx)(t.em,{children:"cannot"})," write to the cluster. Not because a hook caught the command, but because the API server rejects the identity. The write capability is absent from the credential itself."]}),"\n",(0,s.jsxs)(t.p,{children:["This is why the target layer is stronger than the command layer, and why you want both. A hook is ",(0,s.jsx)(t.em,{children:"your"})," code checking ",(0,s.jsx)(t.em,{children:"the command text"}),", it can be too narrow (a command pattern you did not anticipate slips through). RBAC is ",(0,s.jsx)(t.em,{children:"the cluster"})," checking ",(0,s.jsx)(t.em,{children:"the identity"}),", it does not care how the command is spelled; an identity with no write verb cannot write, full stop. The controls compose: the hook catches the obvious destructive commands early and gives a helpful reason; the read-only identity is the backstop that holds even if the hook has a gap."]}),"\n",(0,s.jsx)(t.p,{children:"The target-layer controls you will meet, from your own machine outward:"}),"\n",(0,s.jsxs)(t.ul,{children:["\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"Read-only identities."})," The agent session runs with ",(0,s.jsx)(t.code,{children:"readonly.yaml"}),", a kubeconfig bound to a role with only read verbs. The ",(0,s.jsx)(t.code,{children:"admin.yaml"})," writer identity exists but is used by humans and the reconciler, not the agent's default session."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"IAM / RBAC."}),' Cloud IAM policies and Kubernetes RBAC define what each identity may do at the target. This is where "the agent is read-only in staging and prod" becomes a fact about the credential, not a promise in prose.']}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"Admission controllers."})," The cluster's own gate: a webhook that inspects every write ",(0,s.jsx)(t.em,{children:"the API server receives"})," and can reject it: deny privileged pods, require resource limits, forbid deletes of protected namespaces. This is enforcement the cluster applies to ",(0,s.jsx)(t.em,{children:"everyone"}),", agent or human."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"OPA / Conftest (optional)."})," Policy-as-code you run against manifests and Terraform plans ",(0,s.jsx)(t.em,{children:"before"})," they are applied, the same deterministic-rule idea as the hook, expressed as reusable policy bundles. Optional for this module, but the natural next step when your deny rules outgrow a single shell script."]}),"\n"]}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.strong,{children:"Analogy:"})," the command layer and the target layer are the difference between the bouncer at the door and the vault behind the counter. The bouncer (your hook) checks each person against a list as they try to enter, fast, helpful, and only as good as the list. The vault (the read-only identity, RBAC, admission) does not check anyone against a list; it simply ",(0,s.jsx)(t.em,{children:"cannot be opened"})," without the right key, no matter who walks up or what they say. You want the bouncer for the fast, friendly first pass, and the vault for the guarantee that holds when the bouncer's list has a gap. Defense in depth is not redundancy for its own sake; it is putting the friendly, fallible control and the structural, unarguable control on the ",(0,s.jsx)(t.em,{children:"same"})," door."]}),"\n",(0,s.jsx)(t.h2,{id:"the-change-path-gitops-and-the-reconciler-only-writer",children:"The change path: GitOps and the reconciler-only writer"}),"\n",(0,s.jsxs)(t.p,{children:["The target layer says the agent ",(0,s.jsx)(t.em,{children:"cannot"}),' write to staging or production. That raises an obvious question: then how do changes to those environments ever happen? The answer is the third layer, and it is the one that turns "the agent can\'t write prod" from a limitation into an operating model: ',(0,s.jsx)(t.strong,{children:"GitOps."})]}),"\n",(0,s.jsxs)(t.p,{children:["In GitOps, the desired state of the cluster lives in Git: Helm values, manifests, Argo CD ",(0,s.jsx)(t.code,{children:"Application"})," definitions. A ",(0,s.jsx)(t.strong,{children:"reconciler"})," (Argo CD, in this course) continuously compares the cluster's actual state to Git's declared state and drives the cluster to match. The reconciler is the ",(0,s.jsx)(t.em,{children:"only"})," identity with write access to the cluster. Not the agent. Not the human running the agent. Only the reconciler, and only from ",(0,s.jsx)(t.em,{children:"merged Git state"}),". That single constraint reshapes every change into one path:"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{children:"agent edits manifests \u2192 opens a change on a branch \u2192 human reviews the PR\n  \u2192 approval \u2192 merge to main \u2192 reconciler detects the drift \u2192 reconciler applies\n"})}),"\n",(0,s.jsxs)(t.p,{children:['The agent\'s job ends at "propose." It writes to a branch and stops. Everything after (review, approval, merge, apply) happens through identities and systems the agent does not control. This is the strongest enforcement in the module because it does not just constrain ',(0,s.jsx)(t.em,{children:"what"})," the agent can do to the cluster; it removes the agent from the write path ",(0,s.jsx)(t.em,{children:"entirely"}),". The agent literally has no route to the cluster's state except through a human-reviewed, reconciler-applied Git merge."]}),"\n",(0,s.jsx)(t.mermaid,{value:'flowchart LR\n    AGENT["Agent<br/>(readonly identity)"] --\x3e|"edits + proposes<br/>on a branch"| GIT["Git repo<br/>(desired state)"]\n    GIT --\x3e|"PR"| REVIEW["Human review<br/>+ approval"]\n    REVIEW --\x3e|"merge to main"| MAIN["main branch<br/>(approved desired state)"]\n    MAIN --\x3e|"reconciler detects drift"| RECON["Reconciler (Argo CD)<br/>ONLY cluster writer"]\n    RECON --\x3e|"applies"| CLUSTER["Cluster<br/>(actual state)"]\n    AGENT -. "no direct write path" .-x CLUSTER\n    style RECON fill:#e8f5e9,stroke:#388e3c\n    style AGENT fill:#e3f2fd,stroke:#1565c0\n    style CLUSTER fill:#fff3e0,stroke:#f57c00'}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.strong,{children:"Analogy:"})," GitOps is the difference between letting a contractor renovate your house with their own key, versus a system where the contractor can only ",(0,s.jsx)(t.em,{children:"submit drawings"}),". In the first, the contractor has direct write access to your house, you are trusting their judgment on every cut. In the second, the contractor draws up the change, an inspector reviews it against code, and only ",(0,s.jsx)(t.em,{children:"then"})," does an authorized crew, working from the approved drawings, do the physical work. The contractor never touches the structure directly. The value is not that the contractor is untrusted; it is that ",(0,s.jsx)(t.em,{children:"every"})," change goes through the same reviewed, recorded, reversible path, so a mistake is caught at the drawing stage and the house's actual state always matches an approved plan. The agent is the contractor who submits drawings. The reconciler is the only crew with a key."]}),"\n",(0,s.jsxs)(t.p,{children:["Notice how the reconciler's ",(0,s.jsx)(t.code,{children:"selfHeal"})," closes the loop: if anything changes the cluster ",(0,s.jsx)(t.em,{children:"outside"})," of Git, a manual ",(0,s.jsx)(t.code,{children:"kubectl edit"}),", a stray write, the reconciler detects the drift and reverts it to match the declared state. Git is not just the change path; it is the ",(0,s.jsx)(t.em,{children:"source of truth"}),", and the reconciler enforces it continuously. There is no durable way to change the cluster except by changing Git."]}),"\n",(0,s.jsx)(t.h2,{id:"the-environment-contract-making-the-target-explicit",children:"The Environment Contract: making the target explicit"}),"\n",(0,s.jsxs)(t.p,{children:["The three layers are strongest when the agent, and your scripts, can ",(0,s.jsx)(t.em,{children:"know"})," which environment they are operating against, with no ambiguity. That is the job of the ",(0,s.jsx)(t.strong,{children:"Environment Contract"}),": a single declarative file that names each environment as a ",(0,s.jsx)(t.em,{children:"bundle"})," of explicit target metadata. For ",(0,s.jsx)(t.code,{children:"dev"}),", ",(0,s.jsx)(t.code,{children:"staging"}),", and ",(0,s.jsx)(t.code,{children:"production"}),", it declares the kube-context, the kubeconfig identity, the Terraform backend, and three hard booleans: ",(0,s.jsx)(t.code,{children:"allow_apply"}),", ",(0,s.jsx)(t.code,{children:"allow_delete"}),", ",(0,s.jsx)(t.code,{children:"require_approval"}),"."]}),"\n",(0,s.jsxs)(t.p,{children:['The contract is what turns "be careful about production" into a machine-checkable fact. Its central invariant: ',(0,s.jsx)(t.strong,{children:"dev is the only bundle whose identity can write."})," Staging and production are pinned to the read-only identity, with ",(0,s.jsx)(t.code,{children:"allow_apply: false"})," and ",(0,s.jsx)(t.code,{children:"allow_delete: false"}),". So the environment you are in is not a vibe or a memory, it is a row in a file, and that row says, in booleans, exactly what is permitted."]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-yaml",children:"bundles:\n  dev:\n    kubeconfig: platform/kubeconfigs/admin.yaml    # writer identity\n    allow_apply: true\n    allow_delete: true\n    require_approval: false\n  staging:\n    kubeconfig: platform/kubeconfigs/readonly.yaml # read-only to the agent\n    allow_apply: false                              # writes flow through GitOps\n    allow_delete: false\n  production:\n    kubeconfig: platform/kubeconfigs/readonly.yaml # read-only to the agent\n    allow_apply: false\n    allow_delete: false\n    require_approval: true\n"})}),"\n",(0,s.jsxs)(t.p,{children:["The contract does its real work through a ",(0,s.jsx)(t.strong,{children:"fail-closed mismatch check"}),": ",(0,s.jsx)(t.code,{children:"env-check.sh"})," in the lab. Before an operation, the check reads the ",(0,s.jsx)(t.em,{children:"declared"})," environment and compares it to the ",(0,s.jsx)(t.em,{children:"effective"}),' one: does the kube-context in effect match the bundle you named? Does the identity match? Is the action you are about to take permitted by that bundle\'s booleans? If any answer is "no", or if the check ',(0,s.jsx)(t.em,{children:"cannot determine"})," the answer, it blocks. This directly kills the ",(0,s.jsx)(t.strong,{children:"wrong-target"}),' threat from the table: you cannot run "the staging fix" while accidentally pointed at production, because the declared environment and the effective context will not match, and the mismatch is a block, not a warning. And it is fail-closed for the same reason the hook is: an environment you cannot positively verify is treated as the wrong one.']}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.strong,{children:"Analogy:"})," the Environment Contract is the pre-flight checklist a pilot runs before takeoff. The checklist does not trust the pilot's memory of which runway they are cleared for or whether the flaps are set. It forces an explicit, item-by-item confirmation that the ",(0,s.jsx)(t.em,{children:"declared"})," plan matches the ",(0,s.jsx)(t.em,{children:"actual"})," aircraft state. And a checklist item that cannot be confirmed is not skipped to save time; it halts the takeoff. The contract is that checklist for an ops operation: declare the environment, confirm the effective state matches, confirm the action is permitted, and halt if any item cannot be confirmed. Pilots do not run checklists because they are careless; they run them because the cost of a wrong-target action is catastrophic and human memory is exactly the thing that fails under pressure. Same reasoning, same discipline."]}),"\n",(0,s.jsx)(t.h2,{id:"how-the-layers-compose",children:"How the layers compose"}),"\n",(0,s.jsx)(t.p,{children:"Put the four pieces together and you have defense in depth where each layer covers the layer above it's failure mode:"}),"\n",(0,s.jsxs)(t.ol,{children:["\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"Intent controls"})," (CLAUDE.md, skills) handle the honest common case cheaply."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"The Change Gate hook"})," deterministically blocks the dangerous command patterns, with reasons."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"The Environment Contract check"})," blocks wrong-target and forbidden actions before they run."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"Read-only identities + GitOps"})," are the structural backstop: even if every control above had a gap, the agent's credential cannot write staging or production, and the only write path is a reviewed Git merge applied by the reconciler."]}),"\n"]}),"\n",(0,s.jsxs)(t.p,{children:["The lesson of the whole module compresses to this: ",(0,s.jsx)(t.strong,{children:"do not make the destructive action depend on the agent choosing not to do it."})," Make it depend on a deterministic script, a read-only credential, and a reconciler that is the only writer, controls that hold when the model's judgment does not, because they are not judgment at all."]}),"\n",(0,s.jsx)(t.h2,{id:"what-youll-do-in-the-lab",children:"What you'll do in the lab"}),"\n",(0,s.jsxs)(t.p,{children:["You will build the ",(0,s.jsx)(t.strong,{children:"Change Gate + Environment Contract"})," and prove every control holds:"]}),"\n",(0,s.jsxs)(t.ol,{children:["\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"Build the Change Gate hook"}),": a deterministic ",(0,s.jsx)(t.code,{children:"PreToolUse"})," script that denies production apply, protected-namespace deletion, wildcard mutations, secret output, and ",(0,s.jsx)(t.code,{children:"docker push"}),", each with a reason, fail-closed on error."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsxs)(t.strong,{children:["Wire it into ",(0,s.jsx)(t.code,{children:"settings.json"})]}),": a ",(0,s.jsx)(t.code,{children:"PreToolUse"})," matcher on ",(0,s.jsx)(t.code,{children:"Bash"}),", so every command the agent proposes passes through the gate."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"Test each denial"}),": drive the gate with real dangerous commands and watch them fail with clear messages; drive it with safe commands and watch them pass. Confirm it fails closed on bad input."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"Build the environment bundles"}),": extend the Environment Contract with dev/staging/production target metadata, read-only identities for non-dev, and the ",(0,s.jsx)(t.code,{children:"allow_apply"}),"/",(0,s.jsx)(t.code,{children:"allow_delete"})," booleans."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"Implement the fail-closed mismatch check"}),": ",(0,s.jsx)(t.code,{children:"env-check.sh"})," that blocks when the declared environment does not match the effective context, or when the action is forbidden by the bundle."]}),"\n",(0,s.jsxs)(t.li,{children:[(0,s.jsx)(t.strong,{children:"Set up the GitOps path"}),": use the local Git remote and the Argo CD ",(0,s.jsx)(t.code,{children:"Application"}),"s to show the reconciler as the only cluster writer; the agent proposes on a branch and stops."]}),"\n"]}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.code,{children:"grade.sh m8"})," certifies the gate blocks what it must and allows what it must, the bundles are declared with read-only non-prod identities, and the mismatch check is genuinely fail-closed. The Deep Dive then runs the ",(0,s.jsx)(t.strong,{children:"Destructive Shortcut"})," incident, ",(0,s.jsx)(t.em,{children:'"clean up old resources and fix production"'}),", against everything you built, and you will see the enforcement layer turn a dangerous, ambiguous instruction into a safe, evidence-first, GitOps-routed response, with no direct production write anywhere in it."]})]})}function u(e={}){let{wrapper:t}={...(0,r.R)(),...e.components};return t?(0,s.jsx)(t,{...e,children:(0,s.jsx)(c,{...e})}):c(e)}},7684(e,t,n){n.d(t,{A:()=>r});var o=n(4848);n(6540);var s=n(6497);function r({src:e,title:t="Slides"}){let n=(0,s.Ay)(e);return(0,o.jsxs)("div",{className:"deck_nWXz",children:[(0,o.jsxs)("div",{className:"bar_dOoE",children:[(0,o.jsxs)("span",{className:"label_ZSLR",children:["\u25B6 ",t]}),(0,o.jsx)("a",{className:"full_PrDQ",href:n,target:"_blank",rel:"noopener noreferrer",children:"Open fullscreen \u2197"})]}),(0,o.jsx)("div",{className:"frame_M4DO",children:(0,o.jsx)("iframe",{src:n,title:t,loading:"lazy",allowFullScreen:!0})})]})}},8453(e,t,n){n.d(t,{R:()=>i,x:()=>a});var o=n(6540);let s={},r=o.createContext(s);function i(e){let t=o.useContext(r);return o.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function a(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:i(e.components),o.createElement(r.Provider,{value:t},e.children)}}}]);